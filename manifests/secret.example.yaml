# TEMPLATE ONLY — do not commit a real secret.
# Create the real Secret with kubectl instead:
#   kubectl create secret generic node-red-admin \
#     --from-literal=password-hash='<bcrypt-hash>' -n node-red
apiVersion: v1
kind: Secret
metadata:
  name: node-red-admin
  namespace: node-red
type: Opaque
stringData:
  password-hash: "$2b$08$REPLACE_WITH_YOUR_BCRYPT_HASH"
